# PROMPT COMPLETO – SISTEMA RAYSSA OLIVEIRA (COM COMENTÁRIOS E FLEXIBILIDADE)

## OBJETIVO GERAL
Você é um desenvolvedor sênior e também **professor de programação**. Implemente todas as funcionalidades abaixo no sistema existente (arquivos: `app.js`, `sistema.html`, `cliente.html`, `cliente.js`, `styles.css`, `landing.js`, `firebase-config.js`). O sistema é usado por uma nail designer (atualmente "Rayssa Oliveira", mas o nome deve ser **totalmente configurável** via Admin – `state.settings.companyName`). 

**CRITÉRIO FUNDAMENTAL:** Adicione **comentários detalhados em português** em todo o código (JavaScript, HTML, CSS) explicando:
- O que cada função faz
- Por que determinada lógica foi usada
- Como os dados fluem
- Dicas para iniciantes
- Trechos que podem ser personalizados (ex: nome da empresa, cores, taxas)

Os comentários devem ser educativos, para que o proprietário do código (iniciante em programação) possa aprender e futuramente fazer alterações sozinho.

---

## 1. PACOTES – EXCLUSÃO COM CONFIRMAÇÃO
- [x] Botão "Excluir pacote" já existe, mas garantir que ao excluir:
  - Remova o pacote de `state.pacotes`.
  - Pergunte se deseja excluir agendamentos vinculados. Se sim, exclua-os; se não, apenas desvincule (pacoteId = "", usarPacote = false, resetar pagamento).
  - Remova o lançamento financeiro do pacote (origem "pacote").
  - Atualize a tela.
  - Adicionar comentários explicando o fluxo de exclusão em cascata.

## 2. SISTEMA DE PAGAMENTO (TAXAS/DESCONTOS PERSONALIZADOS)
### 2.1 Configurações no Admin
- Campos no `adminForm` já existem: `taxaDebito`, `taxaCredito`.
- **Remover** qualquer campo de desconto geral para dinheiro/pix (não existe mais).
- Comentar que as taxas de cartão são aplicadas no pagamento.

### 2.2 Modal de pagamento (`#paymentModal`)
- Ao selecionar **Dinheiro** ou **Pix**, exibir campos de desconto (tipo: porcentagem ou valor fixo, valor numérico).
- Ao selecionar **Débito** ou **Crédito**, ocultar os campos de desconto e aplicar a taxa percentual configurada.
- O cálculo deve usar a função `calculatePaymentValues` (comentar a lógica).
- Exibir resumo: valor bruto, desconto/taxa, valor líquido.
- Ao salvar:
  - Atualizar `statusPagamento`, `formaPagamento`, `valorLiquido`, `dataPagamento`, etc.
  - **Não alterar** o `status` do serviço (apenas `statusPagamento`).
  - Gerar/remover lançamento financeiro conforme `statusPagamento === "pago"`.
- Validar: se `statusPagamento === "pago"` e forma de pagamento vazia, exibir erro.

### 2.3 Forma de pagamento no formulário de agendamento (`#appointmentForm`)
- Já existe `#appointmentPaymentMethod` e `#appointmentPaymentStatus`.
- Adicionar campos de desconto (somente para dinheiro/pix) com os mesmos controles do modal.
- Comentar como o valor líquido é calculado e armazenado.

## 3. FLUXO DE PAGAMENTO E CONCLUSÃO (SEM DUPLICAÇÃO)
- **Remover** o botão "Concluir serviço" que existia anteriormente. No card do agendamento, manter apenas o botão **"Pagamento"**.
- O botão "Pagamento" abre o modal de pagamento. Após salvar, se `statusPagamento === "pago"`, alterar o `status` do serviço para "Concluído" (se não estiver). Se `statusPagamento === "pendente"`, manter o status original.
- O seletor de status manual permanece, mas ao alterar para "Concluído", **não** abrir modal de pagamento automaticamente.
- Comentar a separação entre "status do serviço" e "status do pagamento".

## 4. ALERTAS DE PAGAMENTO (RODAPÉ ROLANTE)
- O alerta (`#paymentAlertBar`) deve mostrar **apenas** agendamentos com:
  - `status === "Concluído"` e `statusPagamento === "pendente"`
  - `status === "Pendente confirmação"`
  - Atrasados (status Agendado/Confirmado com data passada)
- **Corrigir**: remover pendentes de pagamento para agendamentos futuros não concluídos.
- A barra de alerta deve ser configurável no Admin: cor de fundo (campo `settingAlert`).
- Comentar a lógica de filtro e animação.

## 5. SINALEIROS COLORIDOS (CALENDÁRIO E LISTA)
- Função `getStatusIcon(appointment)` já existe. Garantir que retorne:
  - 🟢 `statusPagamento === "pago"`
  - 🔴 `status === "Concluído"` e `statusPagamento === "pendente"` ou atrasado
  - 🟡 `status === "Agendado"` (futuro)
  - 🟠 `status === "Confirmado"` (futuro)
  - ⚫ `status === "Cancelado"`
  - 🔵 `status === "Pendente confirmação"`
- Comentar cada condição.

## 6. PERSONALIZAÇÃO COMPLETA (BRANDING)
- Tela de login deve exibir `companyName` e logo (já feito via `applySettings`).
- Título da página usa `companyName()`.
- **Backup**: nome do arquivo `backup-{brandSlug()}-{data}.json`.
- **WhatsApp**: mensagem inclui status de pagamento e usa `companyName()`.
- **Relatórios PDF**: título com `companyName()`.
- **COMENTAR** que para trocar o nome da empresa, basta alterar no Admin → "Nome da empresa". Todas as referências se atualizam automaticamente.

## 7. PACOTES – CRÉDITOS POR SERVIÇO
- O sistema já foi migrado para usar `creditos` como array de serviços com quantidade.
- Verificar migração de pacotes antigos (`peMaoTotal`, `maoTotal`) dentro de `migrateState`.
- No agendamento, ao usar pacote, listar serviços disponíveis do pacote.
- Comentar a estrutura de créditos e como o consumo é calculado.

## 8. GERENCIAMENTO DE IMAGENS (UPLOAD DO PC/CELULAR)
- Logo da empresa: já funciona via Base64.
- **Banner da landing page**: adicionar campo de upload no editor da landing (convertendo para Base64) e salvar em `state.landingContent.heroImage`.
- **Fotos do portfólio**: permitir upload substituindo URL.
- Comentar o processo de conversão para Base64 e armazenamento.

## 9. MODAIS (FUNCIONÁRIOS E OUTROS) – FECHAR/ERRO
- Ao salvar, se erro (ex: senha < 6), modal permanece aberto e exibe toast com erro. Se sucesso, fecha modal.
- Comentar a lógica de validação e prevenção de fechamento.

## 10. TOAST – CORRIGIR SUMIÇO
- O toast deve desaparecer completamente após 2,6s. No CSS, ajustar `transform` e `bottom` no mobile para não ficar sobre o menu.
- Comentar a classe `.toast` e a animação.

## 11. REMOVER CAMPOS MANUAIS DE HORÁRIO (DATETIME-LOCAL)
- **Remover** `#appointmentStart` e `#appointmentEnd` do formulário de agendamento.
- **Adicionar** seletor de data e grade de horários disponíveis (blocos de 30 min, 8h–19h, respeitando duração do serviço).
- Criar função `renderAvailableTimeSlots(serviceId, date)`.
- Comentar a lógica de geração de horários e verificação de conflitos.

## 12. LOGIN/CADASTRO MOBILE – BOTÃO GOOGLE
- Corrigir login com Google (provedor deve estar ativo no Firebase). Se não configurado, exibir mensagem.
- Comentar o fluxo de autenticação e a necessidade de habilitar no Firebase Console.

## 13. INTERLIGAÇÃO FINANCEIRO ↔ AGENDAMENTO
- Ao editar lançamento financeiro de origem "agendamento", atualizar agendamento correspondente (`syncAppointmentFromFinance`).
- Ao excluir, resetar pagamento.
- Comentar a sincronização bidirecional.

## 14. MENU CELULAR – ROLAGEM HORIZONTAL (SEM BOTÃO "MAIS")
- Já implementado no CSS com `overflow-x: auto`. Remover elementos `.mobile-more` do HTML.
- Comentar que no mobile todos os ícones ficam em uma linha rolável.

## 15. ALERTA ROLANTE – PERSONALIZAÇÃO
- A cor da barra de alerta deve vir do Admin (campo `settingAlert`). Aplicar via variável CSS `--alert`.
- Comentar como a variável é aplicada.

## 16. EDIÇÃO DO SITE PÚBLICO (LANDING PAGE) – COMPLETA
- O editor da landing já existe em `sistema.html`. Salvar conteúdo em `state.landingContent`.
- O `landing.js` deve carregar esse conteúdo e preencher a página `index.html`. Criar se não existir.
- Adicionar suporte a upload de imagens para banner e portfolio (Base64).
- Comentar o fluxo de edição e salvamento.

## 17. VALIDAÇÃO DE SENHA (6 CARACTERES)
- Já implementada. Garantir mensagem de erro clara.

## 18. EXCLUSÃO DE CLIENTE (SEGURA)
- Soft delete (`clienteAtivo = false`). Manter agendamentos.
- Comentar por que não excluir fisicamente.

## 19. COMENTÁRIOS DIDÁTICOS EM PORTUGUÊS (OBRIGATÓRIO)
- **Todos os arquivos** (`app.js`, `cliente.js`, `landing.js`, `styles.css`, `sistema.html`, `cliente.html`, `index.html`) devem ter comentários explicativos em português, incluindo:
  - Cabeçalho do arquivo com descrição do propósito.
  - Explicação de variáveis globais, funções, eventos, manipulação de DOM, interação com Firebase, cálculo de valores, etc.
  - Observações sobre como personalizar nomes, cores, taxas.
  - Dicas de boas práticas e referências para aprendizado.
- Os comentários devem ser escritos de forma clara, para um iniciante entender o raciocínio por trás do código.

## 20. FLEXIBILIDADE PARA TROCAR O NOME DA EMPRESA
- Centralize todas as strings de marca em `state.settings.companyName` e use a função `companyName()` em vez de strings literais.
- No código, nunca escrever "Rayssa Oliveira" diretamente (exceto em comentários ou valores padrão iniciais). Use `companyName()`.
- Comentar que para trocar o nome, basta acessar Admin → "Nome da empresa" e salvar. O sistema atualiza automaticamente título, backups, WhatsApp, PDF, etc.

## 21. TESTES MANUAIS (OBRIGATÓRIOS APÓS IMPLEMENTAÇÃO)
- [ ] Login com admin e funcionário.
- [ ] Criar serviço, cliente, agendamento, registrar pagamento com desconto/taxa.
- [ ] Verificar alerta apenas para concluídos pendentes.
- [ ] Verificar ícones coloridos.
- [ ] Backup com nome dinâmico (baseado no nome da empresa).
- [ ] WhatsApp com status de pagamento e nome correto.
- [ ] Excluir pacote com e sem agendamentos.
- [ ] Editor da landing salvar imagens via upload e refletir na página pública.
- [ ] Menu mobile rolável.
- [ ] Toast sumir completamente.

## 22. ENTREGÁVEIS
- Código fonte completo comentado em português.
- Instruções de deploy (Firebase, regras de segurança).
- Breve guia de personalização (como trocar nome, cores, taxas).

## OBSERVAÇÃO FINAL
Lembre-se: o código deve ser educativo e funcionar perfeitamente. Ao final, o proprietário (iniciante) deve ser capaz de entender o que cada parte faz e se sentir confortável para fazer pequenas alterações.

**Desenvolvedor responsável:** Rafael Aguiar Ribeiro  
**Sistema:** Rayssa Oliveira Gestão (personalizável para qualquer nome)